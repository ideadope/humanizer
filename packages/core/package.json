{
  "name": "@ideadope/humanizer",
  "version": "1.1.1",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/index.js",
    "dist/index.d.ts",
    "dist/wasm_exec.js"
  ],
  "scripts": {
    "clean": "rm -rf dist",
    "build:ts": "tsc -p ./tsconfig.json",
    "copy:glue": "cp src/wasm_exec.js dist/wasm_exec.js",
    "build": "npm run clean && mkdir -p dist && npm run build:ts && npm run copy:glue"
  },
  "devDependencies": {
    "@types/node": "^25.5.2"
  }
}
