{
  "name": "@ideadope/humanizer",
  "version": "1.0.2",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/ideadope/humanizer.git",
    "directory": "packages/core"
  },
  "bugs": {
    "url": "https://github.com/ideadope/humanizer/issues"
  },
  "homepage": "https://github.com/ideadope/humanizer#readme",
  "scripts": {
    "clean": "rm -rf dist",
    "build:wasm": "tinygo build -o dist/humanizer.wasm -target wasm ./src/main.go",
    "build:ts": "tsc -p ./tsconfig.json",
    "copy:glue": "cp src/wasm_exec.js dist/wasm_exec.js",
    "build": "npm run clean && mkdir -p dist && npm run build:wasm && npm run build:ts && npm run copy:glue"
  },
  "devDependencies": {
    "@types/node": "^25.5.2",
    "typescript": "^6.0.2"
  }
}
