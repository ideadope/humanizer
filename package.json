{
  "name": "@ideadope/humanizer-root",
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "build:packages": "turbo build --filter='./packages/*'",
    "build:apps": "turbo build --filter='./apps/*'",
    "ship": "turbo build --filter='./packages/*' && pnpm -r --filter='./packages/*' exec pnpm version patch && pnpm -r --filter='./packages/*' publish --access public --no-git-checks"
  },
  "devDependencies": {
    "turbo": "^2.9.5"
  }
}